/*
 * UniverseInfo
 * Classe che gestisce le informazioni su ciò che contiene l'universo
 * Attuale contiene le informazioni sui pianeti (posizione, numero di lune, dimensione, texture)
 */
 
UniverseInfo = function (planetsInfo) {
	this.planetsInfo = planetsInfo;
}